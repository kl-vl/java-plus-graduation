namespace ru.yandex.practicum.ewm.stats.avro;
//TODO @namespace("ru.yandex.practicum.ewm.stats.avro")
// Optional: main schema (using it creates a .avsc equivalent file)
schema ActionTypeAvro;

enum ActionTypeAvro {
	VIEW, REGISTER, LIKE
}

record UserActionAvro {
	long userId;
	long eventId;
	ActionTypeAvro actionType;
	timestamp_ms timestamp;
}